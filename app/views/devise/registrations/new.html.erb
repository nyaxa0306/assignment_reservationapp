<div class="container">
  <h2 class="page-title">新規登録</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <%= f.label :name, "ユーザー名", class: "required-field" %><br />
      <%= f.text_field :name, autofocus: true, class: "input-field" %>
    </div>

    <div class="field">
      <%= f.label :email, "メールアドレス", class: "required-field" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input-field" %>
    </div>

    <div class="field">
      <%= f.label :password, "パスワード", class: "required-field" %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> 文字以上)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password", class: "input-field" %>
    </div>

    <div class="field">
      <%= f.label :password_confirmation, "確認用パスワード", class: "required-field" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "input-field" %>
    </div>

    <div class="actions">
      <%= f.submit "新しいアカウントを作成", class: "actions-btn" %>
    </div>
  <% end %>

  <%= link_to new_user_session_path do %>
    <button class="another-link">ログインはこちら</button>
  <% end %>
</div>
